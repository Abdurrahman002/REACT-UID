import React from 'react';

const Display = ({ input }) => {
  return (
    <div className="display">
      <div className="input">{input}</div>
    </div>
  );
};

export default Display;
